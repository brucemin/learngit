1.在逻辑运算符中的and运算符，A and B，计算机首先运算A，如果A的值是true，再计算B，并输出B的结果，如果A的值是False，就不计算B了。而不是计算and两边的值，使运算量减少。
2.c=b==5(b=1,c=5) 运行输出c结果是False,开始并未理解，后来看书明白(b == 5)返回值False,再将结果赋给c    原是知识点掌握不足，需努力。

总结：对算术运算符、比较运算符、逻辑运算符有了全面了解，且运算优先级依次递减。python与C语言相比没有了自增运算符，且在python中，只要值相同，变量名相同，他们的内存地址是不变的，而C语言值的存储是以变量名来区分的，一个变量具有独立的地址空间单位。